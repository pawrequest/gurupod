2212

"""

update redditbot
monitor.py:
    ++ launch_monitor_jobs()
        + flair submission_write_optional()
        + assign post to gurus in db
        + log

++ add [gurus] to wiki and post writers

listen for new episodes
    + passive receive signal
    ? email notifications
        ? pocketcast?
    ? convert email notificatoon to httprequest -> api
"""

"""

########################

Optional Dependencies¶

Used by Pydantic:

    email_validator - for email validation.
    pydantic-settings - for settings management.
    pydantic-extra-types - for extra types to be used with Pydantic.
orjson - Required if you want to use ORJSONResponse.
